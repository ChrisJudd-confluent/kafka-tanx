# KafkaTanx — Claude Context

## What this project is

KafkaTanx is a fork of `/Users/chris/Projects/tanx` (an Amiga-style two-player tank artillery game using olc::PixelGameEngine). The only change from the original is replacing the TCP LAN networking with **Apache Kafka topics on Confluent Cloud**. Chris works at Confluent and uses this as a demo product.

**The original tanx/ repo must never be modified.** The twins play that version without Kafka.

---

## Confluent Cloud cluster

| Resource | Value |
|---|---|
| Kafka bootstrap | `pkc-41wq6.eu-west-2.aws.confluent.cloud:9092` |
| Schema Registry | `https://psrc-zgxr5eq.eu-west-2.aws.confluent.cloud` |
| Region | AWS eu-west-2 |

---

## Architecture

```
Player 1 (HOST)              Confluent Cloud              Player 2 (CLIENT)
────────────────             ───────────────              ─────────────────
HOST generates code  ──►  kafkatanx-sessions  ◄──  CLIENT enters code
Both subscribe       ──►  kafkatanx-gameplay  ◄──  Both subscribe
MATCH_START/ROUND_START ──►                   ──►  receive & apply
TURN_ACTION          ──►                   ◄──     TURN_ACTION
TURN_RESULT          ──►                   ──►     receive & snap

[HOST only — after each shot]
ShotEvent  ──►  kafkatanx-shots   ──►  Flink
RoundEvent ──►  kafkatanx-rounds  ──►  Flink
GameEvent  ──►  kafkatanx-games   ──►  Flink  ──►  leaderboards / dashboards
```

**HOST authority model** — same as the TCP version. HOST resolves physics, sends TURN_RESULT. Analytics events (shots/rounds/games) written by HOST only.

**Player identity** — persistent UUID stored in `client-kafka.ini`. Generated on first launch. Display name prompted in-game. Both stored in `client-kafka.ini [player]`.

**Game code** — 6-char unambiguous alphanumeric (charset excludes I, O, 0, 1). Generated by HOST, shared out-of-band. Consumer group per session: `kafkatanx-{game_code}-{player_id}`.

---

## Topics

| Topic | Cleanup | Partitions | Purpose |
|---|---|---|---|
| `kafkatanx-sessions` | compact | 6 | Session registry, key=game_code |
| `kafkatanx-gameplay` | delete 24h | 12 | Live game traffic, key=game_code |
| `kafkatanx-players` | compact | 6 | Player identity, key=player_id |
| `kafkatanx-shots` | delete 90d | 12 | Analytics, key=game_code-round-turn |
| `kafkatanx-rounds` | delete 90d | 6 | Analytics, key=game_code-round |
| `kafkatanx-games` | delete 90d | 6 | Analytics, key=game_code |

---

## Key source files

| File | Purpose |
|---|---|
| `kafkatanx.cpp` | Main game — copy of tanx.cpp, TCP section NOT YET replaced |
| `kafka_net.h / .cpp` | KafkaNet class — librdkafka producer/consumer wrapper |
| `avro_codec.h` | Hand-rolled Avro binary encode/decode + Confluent wire format (no avro-cpp dependency) |
| `avro_codec.cpp` | Empty translation unit |
| `schemas/*.avsc` | Six Avro schemas registered in Schema Registry |
| `client-kafka.ini` | Confluent Cloud credentials + player identity (committed to git) |
| `terraform/` | Provisions topics, schemas, service account, ACLs on Confluent Cloud |

---

## Avro / wire format

Binary encoding is hand-rolled in `avro_codec.h` (AvroWriter / AvroReader). No avro-cpp or Boost dependency. Uses Confluent wire format: `[0x00][4-byte big-endian schema ID][Avro payload]`. Admin pre-registers schemas; schema IDs come from `client-kafka.ini [schema-ids]`.

---

## Terraform (terraform/)

Provisions everything on Confluent Cloud in one `terraform apply`:
- 6 topics with correct policies
- 6 Avro schemas registered
- `kafkatanx-client` service account with scoped ACLs
- Player API key (Kafka) and player API key (SR) — outputs a ready-to-paste `client-kafka.ini` snippet

**To run:**
```sh
cd terraform
cp terraform.tfvars.example terraform.tfvars
# fill in cloud API key + environment ID
terraform init && terraform apply
terraform output client_ini_snippet   # paste into client-kafka.ini
```

---

## Current status (as of 2026-07-05)

- [x] Scaffold committed and builds cleanly (librdkafka, hand-rolled Avro)
- [x] All 6 Avro schemas written (`schemas/`)
- [x] `kafka_net.h / .cpp` — full KafkaNet implementation
- [x] `avro_codec.h` — full AvroWriter/AvroReader + AvroWire::Wrap/Unwrap
- [x] Terraform written — topics, schemas, IAM, outputs
- [x] `terraform apply` run against the live cluster (env `env-7mm81w`, cluster `lkc-9kkv5o7`) — topics, schemas, service accounts, ACLs all created
- [x] `client-kafka.ini` — cluster endpoints, API keys, and schema IDs all filled in
- [x] KafkaNet wired into kafkatanx.cpp — TCP socket code replaced with Kafka gameplay/session pipes; HOST-only ShotEvent/RoundEvent/GameEvent analytics added. Builds cleanly; smoke-tested (launches, connects to Confluent Cloud, generates+persists player identity)
- [ ] **NEXT: End-to-end test** — HOST generates code → CLIENT joins → game plays → analytics appear in Confluent Cloud (needs two instances/machines with a display; not yet run interactively)

---

## Build

```sh
# macOS
brew install librdkafka libpng
make

# Linux
sudo apt install librdkafka-dev libpng-dev libx11-dev libgl1-mesa-dev
make

# Windows (MSYS2 UCRT64 shell)
pacman -S mingw-w64-ucrt-x86_64-gcc make mingw-w64-ucrt-x86_64-librdkafka
make
```
